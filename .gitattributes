# Keep the worktree LF everywhere so `bun run format:check` (prettier, endOfLine lf)
# behaves the same on Windows as on CI. Without this, core.autocrlf=true checks
# files out as CRLF and prettier flags every file.
* text=auto eol=lf
